# Enables the $connection_upgrade variable used for WebSocket proxying.
# Install: copy to /etc/nginx/conf.d/websocket-upgrade.conf  (conf.d is inside http{})
# Then: nginx -t && systemctl reload nginx

map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
